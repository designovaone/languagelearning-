# Test fixtures

Small, committed inputs for the suite (PLAN.md §12): a 10-row corpus, a 500-row
review log, 40 labelled answers. `.csv` files here are explicitly un-ignored by
`.gitignore`, and the privacy guard checks that exemption still holds.
